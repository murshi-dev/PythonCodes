# to find the average temperature
maxTemp = float(input("Enter the max temperature: "))
minTemp = float(input("Enter the min temperature: "))
average = (maxTemp + minTemp) / 2
print("Average of ", maxTemp, " and ", minTemp, " is ", average)
